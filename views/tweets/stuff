    <!-- 
    <form action="/tweets" method="POST">
        <label>
          <input type="text" name="name" class="comment" placeholder="What is happening?!" autocomplete="off">
        </label>
        <label>
          <button type="submit" class="postBtn">Post</button>
          <ul>
            <% tweets.forEach(tweet => { %>
              <li>
                <%= tweet.name %>
              </li>
            <% }) %>
          </ul>
</div> -->
<!-- 
&nbsp;
<img class="comment-photo" src="<%= twwet.owner.avatar %>" alt="avatar of commenter">
<a href="/profiles/<%= tweet.owner._id %>">
  <%= tweet.owner.name %>
</a>
&nbsp;at -->